#include "main.h"

/**
 * _isupper - Checks for whether there's an uppercase character
 * @c: The character being checked
 * 
 * Return: C is returned as 1 if uppercase, 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
