#include "main.h"

/**
 * _islower - Checks if there is a lowercase character
 * @c: The character being check
 * Return: c is 1 if lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
